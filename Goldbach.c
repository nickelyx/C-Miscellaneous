#include <stdio.h>
#include <math.h>
#include <assert.h>

int Goldbach(int, int);

int main(void)
{
	int n = 8;
	int primes = 0;
	int sluttal;

	printf("angiv et sluttal");
	scanf("%d", &sluttal);
	

	return 0;
}

int Goldbach(int del1, int del2)
{
	int del1 = tal -3;
	int del2 = 3

	while(del1 >= del2)
	{
		if(primes(del1))
		{
			if(primes(del2))
			{
				printf("%d", tal)
			}
		}
		del1--;
		del2++;
	}
}